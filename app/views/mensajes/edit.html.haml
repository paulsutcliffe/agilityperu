- page_title!('Editando mensaje')

#region-mensajes.container

  .region
    .region-title
      %h1
        Editando
        %b
          mensaje

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @mensaje, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
