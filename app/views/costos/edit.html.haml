- page_title!('Editando costo')

#region-costos.container

  .region
    .region-title
      %h1
        Editando
        %b
          costo

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
