= form_for @visa, :html => { class: 'form-horizontal', role: 'form' } do |f|
  - if @visa.errors.any?
    #error_explanation
      %h2= "#{pluralize(@visa.errors.count, "error")}"
      %ul
        - @visa.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :titulo, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :titulo, class: 'form-control'
  .form-group
    = f.label :imagen, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.file_field :imagen, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
