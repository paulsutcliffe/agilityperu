#region-duplas.container

  .region
    .region-title
      %h1= page_title!('duplas')

    .region
      - if current_admin
        .admin-control
          = link_to 'Crear Dupla', new_dupla_path, class: 'btn btn-primary'

  .region
    #duplas
      .region
        = render @duplas
