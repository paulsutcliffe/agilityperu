- page_title!('Editando dupla')

#region-duplas.container

  .region
    .region-title
      %h1
        Editando
        %b
          dupla

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @dupla, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
