- page_title!('Editando streaming')

#region-streamings.container

  .region
    .region-title
      %h1
        Editando
        %b
          streaming

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
